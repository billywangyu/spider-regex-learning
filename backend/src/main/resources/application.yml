spring:
  datasource:
    url: jdbc:postgresql://db.lmsvnaarfaavycynqpum.pooler.supabase.co:6543/postgres?user=postgres&password=WwUz0v1fyrN4fRUR&ssl=true
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 5000      # 5 秒超时，快速失败
      initialization-fail-timeout: 5000
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
server:
  port: ${PORT:8080}
logging:
  level:
    com.zaxxer.hikari: DEBUG
    org.springframework.jdbc: DEBUG
    org.postgresql: DEBUG
    org.hibernate: DEBUG